/**
* @file primenum.h
*
*/
#ifndef __PRIMENUM_H__
#define __PRIMENUM_H__

/**
* Calculates the prime number of integer number
* @param[in] num for which we check the prime number condition
* @return 1 and 0 depending on the input
* @note Returns -1 for negative values
*/
int primenum(int num);

#endif /* #ifndef __FACTORIAL_H__ */

